{
  "name": "fastapi",
  "description": "FastAPI application with health endpoint and structured logging",
  "port": 8000,
  "variables": [
    {
      "name": "PROJECT_NAME",
      "description": "Name of the project",
      "default": "sandbox-app",
      "required": true
    },
    {
      "name": "PORT",
      "description": "Port to expose",
      "default": "8000",
      "required": false
    }
  ]
}
